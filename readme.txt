The data folder is the site data set we use
The code folder is what we use, and each model has two codes, one is soil moisture and the other is soil temperature.
At the last main function in each python file, you can get the data of each site by modifying the parameters and the path of the file.